<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>fr.gouv.agriculture.dal.sial.analyse-risque</groupId>
        <artifactId>sial-analyse-risque</artifactId>
        <version>2.0-m1-SNAPSHOT</version>
    </parent>

    <artifactId>sial-analyse-risque-agent-calculernoterisque</artifactId>
    <packaging>jar</packaging>
    <name>sial-analyse-risque-agent-calculernoterisque</name>

    <properties>
        <url-path>release/${project.version}</url-path>
        <departement>o2</departement>
        <aq-base-url>http://service.agriculture.gouv.fr/o2/o2-code-aq/1.2/rules</aq-base-url>

        <project.build.jdk>1.6</project.build.jdk>
        <aspectj.version>1.6.12</aspectj.version>
        <quartz.version>1.8.5</quartz.version>
        <camel.version>2.10.2</camel.version>
        <agent-api.version>1.2</agent-api.version>

        <!-- Tests -->
        <o2junit.version>1.0</o2junit.version>
        <unitils.version>3.3</unitils.version>
        <easymock.version>3.1</easymock.version>
        <junit.version>4.10</junit.version>

        <!-- Commons Orion -->
        <kernel.version>1.7</kernel.version>
        <kernel.impl.version>1.5</kernel.impl.version>
        <o2-io.version>1.8-beta1</o2-io.version>
        <o2-transaction-api.version>1.0</o2-transaction-api.version>
        <o2-transaction-impl.version>1.0</o2-transaction-impl.version>
        <o2-transaction-jdbc.version>1.0</o2-transaction-jdbc.version>
        <o2-jdbc-support.version>1.3</o2-jdbc-support.version>

        <!-- Apache Commons -->
        <commons-lang3.version>3.1</commons-lang3.version>
        <commons-collections.version>3.2.1</commons-collections.version>

        <!-- Logging -->
        <slf4j.version>1.6.4</slf4j.version>
        <logback.version>1.0.0</logback.version>

        <!-- ORS -->
        <ors.version>2.1-beta1</ors.version>
    </properties>

    <profiles>
        
        <!-- Profil de l'environnement de local -->
        <profile>
            <id>local</id>
            <activation>
                <!--<activeByDefault>true</activeByDefault>-->
                <property>
                    <name>env</name>
                    <value>local</value>
                </property>
            </activation>
            <build>
                <resources>
                    <resource>
                        <directory>src/main/resources</directory>
                        <excludes>
                            <exclude>env.properties</exclude>
                            <exclude>logback.xml</exclude>
                            <exclude>env/**</exclude>
                            <exclude>bin/**</exclude>
                            <exclude>tests/**</exclude>
                        </excludes>
                        <includes>
                            <include>**/**</include>
                        </includes>
                    </resource>
                    <resource>
                        <directory>src/main/resources/env/local</directory>
                        <includes>
                            <include>env.properties</include>
                            <include>logback.xml</include>
                            <include>log4j.properties</include>
                            <include>spy.properties</include>
                        </includes>
                    </resource>
                </resources>            
            </build>
        </profile>
                
        
        <!-- Profil de l'environnement de developpement packagé -->
        <profile>
            <id>developpement</id>
            <activation>
                <!--<activeByDefault>true</activeByDefault>-->
                <property>
                    <name>env</name>
                    <value>developpement</value>
                </property>
            </activation>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-jar-plugin</artifactId>
                    </plugin>

                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-install-plugin</artifactId>
                        <configuration>
                            <classifier>developpement</classifier>
                        </configuration>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-deploy-plugin</artifactId>
                        <configuration>
                            <classifier>developpement</classifier>
                        </configuration>
                    </plugin>
                    <plugin>
                        <artifactId>maven-assembly-plugin</artifactId>
                        <configuration>
                            <descriptors>
                                <descriptor>src/main/assembly/application-dev.xml</descriptor>
                            </descriptors>
                            <classifier>developpement</classifier>
                        </configuration>
                        <executions>
                            <execution>
                                <phase>package</phase>
                                <goals>
                                    <goal>attached</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
                <resources>
                    <resource>
                        <directory>src/main/resources</directory>
                        <excludes>
                            <exclude>env.properties</exclude>
                            <exclude>logback.xml</exclude>
                            <exclude>env/**</exclude>
                            <exclude>bin/**</exclude>
                            <exclude>tests/**</exclude>
                        </excludes>
                        <includes>
                            <include>**/**</include>
                        </includes>
                    </resource>
                    <resource>
                        <directory>src/main/resources/env/developpement</directory>
                        <includes>
                            <include>env.properties</include>
                            <include>logback.xml</include>
                            <include>log4j.properties</include>
                            <include>spy.properties</include>
                        </includes>
                    </resource>
                </resources>
            </build>
            <distributionManagement>
                <snapshotRepository>
                    <uniqueVersion>false</uniqueVersion>
                    <id>Work</id>
                    <name>Maven Snapshot Repository</name>
                    <url>dav:https://repos.forge.agriculture/repository/sial-analyse-risque-work</url>
                    <layout>default</layout>
                </snapshotRepository>
                <repository>
                    <uniqueVersion>false</uniqueVersion>
                    <id>Work</id>
                    <name>Maven Internal Repository</name>
                    <url>dav:https://repos.forge.agriculture/repository/sial-analyse-risque-work</url>
                    <layout>default</layout>
                </repository>
                <site>
                    <id>reports.forge.agriculture</id>
                    <url>dav:https://${reports.service.server}/${departement}/${si}/${project.artifactId}/${url-path}/</url>
                </site>
            </distributionManagement>
        </profile>

        <!-- Profil de l'environnement d'integration packagé -->
        <profile>
            <id>integration</id>
            <activation>
                <property>
                    <name>env</name>
                    <value>integration</value>
                </property>
            </activation>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-jar-plugin</artifactId>
                    </plugin>

                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-install-plugin</artifactId>
                        <configuration>
                            <classifier>integration</classifier>
                        </configuration>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-deploy-plugin</artifactId>
                        <configuration>
                            <classifier>integration</classifier>
                        </configuration>
                    </plugin>
                    <plugin>
                        <artifactId>maven-assembly-plugin</artifactId>
                        <configuration>
                            <descriptors>
                                <descriptor>src/main/assembly/application-int.xml</descriptor>
                            </descriptors>
                            <classifier>integration</classifier>
                        </configuration>
                        <executions>
                            <execution>
                                <phase>package</phase>
                                <goals>
                                    <goal>attached</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
                <resources>
                    <resource>
                        <directory>src/main/resources</directory>
                        <excludes>
                            <exclude>env.properties</exclude>
                            <exclude>logback.xml</exclude>
                            <exclude>env/**</exclude>
                            <exclude>bin/**</exclude>
                            <exclude>tests/**</exclude>
                        </excludes>
                        <includes>
                            <include>**/**</include>
                        </includes>
                    </resource>
                    <resource>
                        <directory>src/main/resources/env/integration</directory>
                        <includes>
                            <include>env.properties</include>
                            <include>logback.xml</include>
                            <include>log4j.properties</include>
                            <include>spy.properties</include>
                        </includes>
                    </resource>
                </resources>
            </build>
            <distributionManagement>
                <snapshotRepository>
                    <uniqueVersion>false</uniqueVersion>
                    <id>Work</id>
                    <name>Maven Snapshot Repository</name>
                    <url>dav:https://repos.forge.agriculture/repository/sial-analyse-risque-work</url>
                    <layout>default</layout>
                </snapshotRepository>
                <repository>
                    <uniqueVersion>false</uniqueVersion>
                    <id>Work</id>
                    <name>Maven Internal Repository</name>
                    <url>dav:https://repos.forge.agriculture/repository/sial-analyse-risque-work</url>
                    <layout>default</layout>
                </repository>
                <site>
                    <id>reports.forge.agriculture</id>
                    <url>dav:https://${reports.service.server}/${departement}/${si}/${project.artifactId}/${url-path}/</url>
                </site>
            </distributionManagement>
        </profile>

        <!-- Profil de l'environnement d'Intégration Technique Fonctionnelle -->
        <profile>
            <id>int-tech-fonc</id>
            <activation>
                <property>
                    <name>env</name>
                    <value>int-tech-fonc</value>
                </property>
            </activation>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-jar-plugin</artifactId>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-install-plugin</artifactId>
                        <configuration>
                            <classifier>int-tech-fonc</classifier>
                        </configuration>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-deploy-plugin</artifactId>
                        <configuration>
                            <classifier>int-tech-fonc</classifier>
                        </configuration>
                    </plugin>
                    <plugin>
                        <artifactId>maven-assembly-plugin</artifactId>
                        <configuration>
                            <descriptors>
                                <descriptor>src/main/assembly/application-inttechfonc.xml</descriptor>
                            </descriptors>
                            <classifier>int-tech-fonc</classifier>
                        </configuration>
                        <executions>
                            <execution>
                                <phase>package</phase>
                                <goals>
                                    <goal>attached</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
                <resources>
                    <resource>
                        <directory>src/main/resources</directory>
                        <excludes>
                            <exclude>env.properties</exclude>
                            <exclude>logback.xml</exclude>
                            <exclude>env/**</exclude>
                            <exclude>bin/**</exclude>
                            <exclude>tests/**</exclude>
                        </excludes>
                        <includes>
                            <include>**/**</include>
                        </includes>
                    </resource>
                    <resource>
                        <directory>src/main/resources/env/int-tech-fonc</directory>
                        <includes>
                            <include>env.properties</include>
                            <include>logback.xml</include>
                            <include>log4j.properties</include>
                            <include>spy.properties</include>
                        </includes>
                    </resource>
                </resources>
            </build>
            <distributionManagement>
                <snapshotRepository>
                    <uniqueVersion>false</uniqueVersion>
                    <id>Work</id>
                    <name>Maven Snapshot Repository</name>
                    <url>dav:https://repos.forge.agriculture/repository/sial-analyse-risque-work</url>
                    <layout>default</layout>
                </snapshotRepository>
                <repository>
                    <uniqueVersion>false</uniqueVersion>
                    <id>Work</id>
                    <name>Maven Internal Repository</name>
                    <url>dav:https://repos.forge.agriculture/repository/sial-analyse-risque-work</url>
                    <layout>default</layout>
                </repository>
                <site>
                    <id>reports.forge.agriculture</id>
                    <url>dav:https://${reports.service.server}/${departement}/${si}/${project.artifactId}/${url-path}/</url>
                </site>
            </distributionManagement>
        </profile>

        <!-- Profil de l'environnement de performance packagé -->
        <profile>
            <id>performance</id>
            <activation>
                <property>
                    <name>env</name>
                    <value>performance</value>
                </property>
            </activation>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-jar-plugin</artifactId>
                    </plugin>

                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-install-plugin</artifactId>
                        <configuration>
                            <classifier>performance</classifier>
                        </configuration>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-deploy-plugin</artifactId>
                        <configuration>
                            <classifier>performance</classifier>
                        </configuration>
                    </plugin>
                    <plugin>
                        <artifactId>maven-assembly-plugin</artifactId>
                        <configuration>
                            <descriptors>
                                <descriptor>src/main/assembly/application-perf.xml</descriptor>
                            </descriptors>
                            <classifier>performance</classifier>
                        </configuration>
                        <executions>
                            <execution>
                                <phase>package</phase>
                                <goals>
                                    <goal>attached</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
                <resources>
                    <resource>
                        <directory>src/main/resources</directory>
                        <excludes>
                            <exclude>env.properties</exclude>
                            <exclude>logback.xml</exclude>
                            <exclude>env/**</exclude>
                            <exclude>bin/**</exclude>
                            <exclude>tests/**</exclude>
                        </excludes>
                        <includes>
                            <include>**/**</include>
                        </includes>
                    </resource>
                    <resource>
                        <directory>src/main/resources/env/private/performance</directory>
                        <includes>
                            <include>env.properties</include>
                            <include>logback.xml</include>
                            <include>log4j.properties</include>
                            <include>spy.properties</include>
                        </includes>
                    </resource>
                </resources>
            </build>
            <distributionManagement>
                <snapshotRepository>
                    <uniqueVersion>false</uniqueVersion>
                    <id>Private</id>
                    <name>Maven Snapshot Repository</name>
                    <url>dav:https://repos.forge.agriculture/repository/sial-analyse-risque-private</url>
                    <layout>default</layout>
                </snapshotRepository>
                <repository>
                    <uniqueVersion>false</uniqueVersion>
                    <id>Private</id>
                    <name>Maven Internal Repository</name>
                    <url>dav:https://repos.forge.agriculture/repository/sial-analyse-risque-private</url>
                    <layout>default</layout>
                </repository>
                <site>
                    <id>reports.forge.agriculture</id>
                    <url>dav:https://${reports.service.server}/${departement}/${si}/${project.artifactId}/${url-path}/</url>
                </site>
            </distributionManagement>
        </profile>
        
        <!-- Profil de l'environnement d'auto-formation packagé -->
        <profile>
            <id>formation-autoform</id>
            <activation>
                <property>
                    <name>env</name>
                    <value>formation-autoform</value>
                </property>
            </activation>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-jar-plugin</artifactId>
                    </plugin>

                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-install-plugin</artifactId>
                        <configuration>
                            <classifier>formation-autoform</classifier>
                        </configuration>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-deploy-plugin</artifactId>
                        <configuration>
                            <classifier>formation-autoform</classifier>
                        </configuration>
                    </plugin>
                    <plugin>
                        <artifactId>maven-assembly-plugin</artifactId>
                        <configuration>
                            <descriptors>
                                <descriptor>src/main/assembly/application-frm-autoform.xml</descriptor>
                            </descriptors>
                            <classifier>formation-autoform</classifier>
                        </configuration>
                        <executions>
                            <execution>
                                <phase>package</phase>
                                <goals>
                                    <goal>attached</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
                <resources>
                    <resource>
                        <directory>src/main/resources</directory>
                        <excludes>
                            <exclude>env.properties</exclude>
                            <exclude>logback.xml</exclude>
                            <exclude>env/**</exclude>
                            <exclude>bin/**</exclude>
                            <exclude>tests/**</exclude>
                        </excludes>
                        <includes>
                            <include>**/**</include>
                        </includes>
                    </resource>
                    <resource>
                        <directory>src/main/resources/env/private/formation-autoform</directory>
                        <includes>
                            <include>env.properties</include>
                            <include>logback.xml</include>
                            <include>log4j.properties</include>
                            <include>spy.properties</include>
                        </includes>
                    </resource>
                </resources>
            </build>
            <distributionManagement>
                <snapshotRepository>
                    <uniqueVersion>false</uniqueVersion>
                    <id>Private</id>
                    <name>Maven Snapshot Repository</name>
                    <url>dav:https://repos.forge.agriculture/repository/sial-analyse-risque-private</url>
                    <layout>default</layout>
                </snapshotRepository>
                <repository>
                    <uniqueVersion>false</uniqueVersion>
                    <id>Private</id>
                    <name>Maven Internal Repository</name>
                    <url>dav:https://repos.forge.agriculture/repository/sial-analyse-risque-private</url>
                    <layout>default</layout>
                </repository>
                <site>
                    <id>reports.forge.agriculture</id>
                    <url>dav:https://${reports.service.server}/${departement}/${si}/${project.artifactId}/${url-path}/</url>
                </site>
            </distributionManagement>
        </profile>
        
        <!-- Profil de l'environnement de formation-formateur packagé -->
        <profile>
            <id>formation-formateur</id>
            <activation>
                <property>
                    <name>env</name>
                    <value>formation-formateur</value>
                </property>
            </activation>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-jar-plugin</artifactId>
                    </plugin>

                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-install-plugin</artifactId>
                        <configuration>
                            <classifier>formation-formateur</classifier>
                        </configuration>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-deploy-plugin</artifactId>
                        <configuration>
                            <classifier>formation-formateur</classifier>
                        </configuration>
                    </plugin>
                    <plugin>
                        <artifactId>maven-assembly-plugin</artifactId>
                        <configuration>
                            <descriptors>
                                <descriptor>src/main/assembly/application-frm-formateur.xml</descriptor>
                            </descriptors>
                            <classifier>formation-formateur</classifier>
                        </configuration>
                        <executions>
                            <execution>
                                <phase>package</phase>
                                <goals>
                                    <goal>attached</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
                <resources>
                    <resource>
                        <directory>src/main/resources</directory>
                        <excludes>
                            <exclude>env.properties</exclude>
                            <exclude>logback.xml</exclude>
                            <exclude>env/**</exclude>
                            <exclude>bin/**</exclude>
                            <exclude>tests/**</exclude>
                        </excludes>
                        <includes>
                            <include>**/**</include>
                        </includes>
                    </resource>
                    <resource>
                        <directory>src/main/resources/env/private/formation-formateur</directory>
                        <includes>
                            <include>env.properties</include>
                            <include>logback.xml</include>
                            <include>log4j.properties</include>
                            <include>spy.properties</include>
                        </includes>
                    </resource>
                </resources>
            </build>
            <distributionManagement>
                <snapshotRepository>
                    <uniqueVersion>false</uniqueVersion>
                    <id>Private</id>
                    <name>Maven Snapshot Repository</name>
                    <url>dav:https://repos.forge.agriculture/repository/sial-analyse-risque-private</url>
                    <layout>default</layout>
                </snapshotRepository>
                <repository>
                    <uniqueVersion>false</uniqueVersion>
                    <id>Private</id>
                    <name>Maven Internal Repository</name>
                    <url>dav:https://repos.forge.agriculture/repository/sial-analyse-risque-private</url>
                    <layout>default</layout>
                </repository>
                <site>
                    <id>reports.forge.agriculture</id>
                    <url>dav:https://${reports.service.server}/${departement}/${si}/${project.artifactId}/${url-path}/</url>
                </site>
            </distributionManagement>
        </profile>
        
        <!-- Profil de l'environnement de formation-stagiaire packagé -->
        <profile>
            <id>formation-stagiaire</id>
            <activation>
                <property>
                    <name>env</name>
                    <value>formation-stagiaire</value>
                </property>
            </activation>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-jar-plugin</artifactId>
                    </plugin>

                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-install-plugin</artifactId>
                        <configuration>
                            <classifier>formation-stagiaire</classifier>
                        </configuration>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-deploy-plugin</artifactId>
                        <configuration>
                            <classifier>formation-stagiaire</classifier>
                        </configuration>
                    </plugin>
                    <plugin>
                        <artifactId>maven-assembly-plugin</artifactId>
                        <configuration>
                            <descriptors>
                                <descriptor>src/main/assembly/application-frm-stagiaire.xml</descriptor>
                            </descriptors>
                            <classifier>formation-stagiaire</classifier>
                        </configuration>
                        <executions>
                            <execution>
                                <phase>package</phase>
                                <goals>
                                    <goal>attached</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
                <resources>
                    <resource>
                        <directory>src/main/resources</directory>
                        <excludes>
                            <exclude>env.properties</exclude>
                            <exclude>logback.xml</exclude>
                            <exclude>env/**</exclude>
                            <exclude>bin/**</exclude>
                            <exclude>tests/**</exclude>
                        </excludes>
                        <includes>
                            <include>**/**</include>
                        </includes>
                    </resource>
                    <resource>
                        <directory>src/main/resources/env/private/formation-stagiaire</directory>
                        <includes>
                            <include>env.properties</include>
                            <include>logback.xml</include>
                            <include>log4j.properties</include>
                            <include>spy.properties</include>
                        </includes>
                    </resource>
                </resources>
            </build>
            <distributionManagement>
                <snapshotRepository>
                    <uniqueVersion>false</uniqueVersion>
                    <id>Private</id>
                    <name>Maven Snapshot Repository</name>
                    <url>dav:https://repos.forge.agriculture/repository/sial-analyse-risque-private</url>
                    <layout>default</layout>
                </snapshotRepository>
                <repository>
                    <uniqueVersion>false</uniqueVersion>
                    <id>Private</id>
                    <name>Maven Internal Repository</name>
                    <url>dav:https://repos.forge.agriculture/repository/sial-analyse-risque-private</url>
                    <layout>default</layout>
                </repository>
                <site>
                    <id>reports.forge.agriculture</id>
                    <url>dav:https://${reports.service.server}/${departement}/${si}/${project.artifactId}/${url-path}/</url>
                </site>
            </distributionManagement>
        </profile>
        
        <!-- Profil de l'environnement de pre-production-fonctionnelle packagé -->
        <profile>
            <id>pre-production-fonctionnelle</id>
            <activation>
                <property>
                    <name>env</name>
                    <value>pre-production-fonctionnelle</value>
                </property>
            </activation>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-jar-plugin</artifactId>
                    </plugin>

                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-install-plugin</artifactId>
                        <configuration>
                            <classifier>pre-production-fonctionnelle</classifier>
                        </configuration>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-deploy-plugin</artifactId>
                        <configuration>
                            <classifier>pre-production-fonctionnelle</classifier>
                        </configuration>
                    </plugin>
                    <plugin>
                        <artifactId>maven-assembly-plugin</artifactId>
                        <configuration>
                            <descriptors>
                                <descriptor>src/main/assembly/application-pprd-fct.xml</descriptor>
                            </descriptors>
                            <classifier>pre-production-fonctionnelle</classifier>
                        </configuration>
                        <executions>
                            <execution>
                                <phase>package</phase>
                                <goals>
                                    <goal>attached</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
                <resources>
                    <resource>
                        <directory>src/main/resources</directory>
                        <excludes>
                            <exclude>env.properties</exclude>
                            <exclude>logback.xml</exclude>
                            <exclude>env/**</exclude>
                            <exclude>bin/**</exclude>
                            <exclude>tests/**</exclude>
                        </excludes>
                        <includes>
                            <include>**/**</include>
                        </includes>
                    </resource>
                    <resource>
                        <directory>src/main/resources/env/private/pre-production-fonctionnelle</directory>
                        <includes>
                            <include>env.properties</include>
                            <include>logback.xml</include>
                        </includes>
                    </resource>
                </resources>
            </build>
            <distributionManagement>
                <snapshotRepository>
                    <uniqueVersion>false</uniqueVersion>
                    <id>Private</id>
                    <name>Maven Snapshot Repository</name>
                    <url>dav:https://repos.forge.agriculture/repository/sial-analyse-risque-private</url>
                    <layout>default</layout>
                </snapshotRepository>
                <repository>
                    <uniqueVersion>false</uniqueVersion>
                    <id>Private</id>
                    <name>Maven Internal Repository</name>
                    <url>dav:https://repos.forge.agriculture/repository/sial-analyse-risque-private</url>
                    <layout>default</layout>
                </repository>
                <site>
                    <id>reports.forge.agriculture</id>
                    <url>dav:https://${reports.service.server}/${departement}/${si}/${project.artifactId}/${url-path}/</url>
                </site>
            </distributionManagement>
        </profile>
        
        <!-- Profil de l'environnement de production packagé -->
        <profile>
            <id>production</id>
            <activation>
                <property>
                    <name>env</name>
                    <value>production</value>
                </property>
            </activation>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-jar-plugin</artifactId>
                    </plugin>

                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-install-plugin</artifactId>
                        <configuration>
                            <classifier>production</classifier>
                        </configuration>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-deploy-plugin</artifactId>
                        <configuration>
                            <classifier>production</classifier>
                        </configuration>
                    </plugin>
                    <plugin>
                        <artifactId>maven-assembly-plugin</artifactId>
                        <configuration>
                            <descriptors>
                                <descriptor>src/main/assembly/application-prod.xml</descriptor>
                            </descriptors>
                            <classifier>production</classifier>
                        </configuration>
                        <executions>
                            <execution>
                                <phase>package</phase>
                                <goals>
                                    <goal>attached</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
                <resources>
                    <resource>
                        <directory>src/main/resources</directory>
                        <excludes>
                            <exclude>env.properties</exclude>
                            <exclude>logback.xml</exclude>
                            <exclude>env/**</exclude>
                            <exclude>bin/**</exclude>
                            <exclude>tests/**</exclude>
                        </excludes>
                        <includes>
                            <include>**/**</include>
                        </includes>
                    </resource>
                    <resource>
                        <directory>src/main/resources/env/private/production</directory>
                        <includes>
                            <include>env.properties</include>
                            <include>logback.xml</include>
                        </includes>
                    </resource>
                </resources>
            </build>
            <distributionManagement>
                <snapshotRepository>
                    <uniqueVersion>false</uniqueVersion>
                    <id>Private</id>
                    <name>Maven Snapshot Repository</name>
                    <url>dav:https://repos.forge.agriculture/repository/sial-analyse-risque-private</url>
                    <layout>default</layout>
                </snapshotRepository>
                <repository>
                    <uniqueVersion>false</uniqueVersion>
                    <id>Private</id>
                    <name>Maven Internal Repository</name>
                    <url>dav:https://repos.forge.agriculture/repository/sial-analyse-risque-private</url>
                    <layout>default</layout>
                </repository>
                <site>
                    <id>reports.forge.agriculture</id>
                    <url>dav:https://${reports.service.server}/${departement}/${si}/${project.artifactId}/${url-path}/</url>
                </site>
            </distributionManagement>
        </profile>

    </profiles>

    <build>
        <plugins>
            <plugin>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <source>${project.build.jdk}</source>
                    <target>${project.build.jdk}</target>
                    <showDeprecation>true</showDeprecation>
                </configuration>
            </plugin>
            <plugin>
                <!--FDA 2015/04 On précise la version du plug-in "exec-maven-plugin" pour éviter d'avoir à mettre "-Denforcer.skip=true" pour exécuter. -->
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <version>1.2.1</version>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>aspectj-maven-plugin</artifactId>
                <version>${aspectj-maven-plugin.version}</version>
                <configuration>
                    <complianceLevel>${aspectj.build.jdk}</complianceLevel>
                    <source>${aspectj.build.jdk}</source>
                    <target>${aspectj.build.jdk}</target>
                    <aspectLibraries>
                        <aspectLibrary>
                            <groupId>fr.gouv.agriculture.o2</groupId>
                            <artifactId>o2-kernel-spring</artifactId>
                        </aspectLibrary>
                        <aspectLibrary>
                            <groupId>fr.gouv.agriculture.o2</groupId>
                            <artifactId>o2-transaction-impl</artifactId>
                        </aspectLibrary>
                    </aspectLibraries>
                </configuration>
                <dependencies>
                    <dependency>
                        <groupId>org.aspectj</groupId>
                        <artifactId>aspectjrt</artifactId>
                        <version>${aspectj.version}</version>
                    </dependency>
                    <dependency>
                        <groupId>org.aspectj</groupId>
                        <artifactId>aspectjtools</artifactId>
                        <version>${aspectj.version}</version>
                    </dependency>
                </dependencies>
                <executions>
                    <execution>
                        <goals>
                            <goal>compile</goal>
                            <goal>test-compile</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-site-plugin</artifactId>
                <inherited>false</inherited>
                <configuration>
                    <siteDirectory>${project.basedir}/src/site/site-default</siteDirectory>
                    <locales>fr</locales>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>cobertura-maven-plugin</artifactId>
                <configuration>
                    <aggregate>true</aggregate>
                    <formats>
                        <format>html</format>
                        <format>xml</format>
                    </formats>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-source-plugin</artifactId>
                <executions>
                    <execution>
                        <goals>
                            <goal>test-jar</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <executions>
                    <execution>
                        <goals>
                            <goal>test-jar</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-pmd-plugin</artifactId>
                <version>2.4</version>
                <dependencies>
                    <dependency>
                        <groupId>fr.gouv.agriculture.o2</groupId>
                        <artifactId>o2-code-aq</artifactId>
                        <version>1.2</version>
                    </dependency>
                </dependencies>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <!-- FDA 2015/04 On précise une version >= 2.12.2 pour éviter le bug "No tests executed" lorsque "Test file" (ok lorsque "Test project" (cf. https://netbeans.org/bugzilla/show_bug.cgi?id=215148). -->
                <version>2.12.2</version>
                <configuration>
                    <argLine>-Xmx256M</argLine>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <repositories>
        <repository>
            <releases>
                <updatePolicy>never</updatePolicy>
                <!-- passé en warn au lieu de fail pour débloquer bonita-client 3.1 qui 
                est systématiquement en checksum error sur archiva/patched. A changer dès 
                que le souci est corrigé sur Archiva -->
                <checksumPolicy>warn</checksumPolicy>
            </releases>
            <snapshots>
                <enabled>false</enabled>
                <updatePolicy>never</updatePolicy>
                <checksumPolicy>fail</checksumPolicy>
            </snapshots>
            <id>Map</id>
            <url>https://repos.forge.agriculture/repository/Map</url>
        </repository>
        <repository>
            <releases>
                <updatePolicy>never</updatePolicy>
                <checksumPolicy>fail</checksumPolicy>
            </releases>
            <snapshots>
                <!-- Il arrive fréquemment qu'un snapshot Orion dépende d'un snapshot 
                des projets o2 commons (io, kernel), notamment lors de la correction de bugs. -->
                <updatePolicy>never</updatePolicy>
                <checksumPolicy>fail</checksumPolicy>
            </snapshots>
            <id>Project</id>
            <url>https://repos.forge.agriculture/repository/Project</url>
        </repository>
    </repositories>

    <pluginRepositories>
        <pluginRepository>
            <releases>
            </releases>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
            <id>Map</id>
            <url>https://repos.forge.agriculture/repository/Map</url>
        </pluginRepository>
    </pluginRepositories>

    <reporting>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-project-info-reports-plugin</artifactId>
                <version>2.1.1</version>
                <inherited>true</inherited>
                <reportSets>
                    <reportSet>
                        <reports>
                            <report>index</report>
                            <report>summary</report>
                        </reports>
                    </reportSet>
                </reportSets>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-javadoc-plugin</artifactId>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jxr-plugin</artifactId>
            </plugin>

            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>cobertura-maven-plugin</artifactId>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-pmd-plugin</artifactId>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-report-plugin</artifactId>
            </plugin>
        </plugins>
    </reporting>

    <dependencies>

        <!-- Tissage -->
        <dependency>
            <groupId>org.aspectj</groupId>
            <artifactId>aspectjrt</artifactId>
            <version>${aspectj.version}</version>
        </dependency>

        <!-- Agent api -->
        <dependency>
            <groupId>fr.gouv.agriculture.o2</groupId>
            <artifactId>agent-api</artifactId>
            <version>${agent-api.version}</version>
        </dependency>
        <!-- Quartz -->
        <dependency>
            <groupId>org.quartz-scheduler</groupId>
            <artifactId>quartz</artifactId>
            <version>${quartz.version}</version>
        </dependency>

        <!-- Commons O2 -->
        <dependency>
            <groupId>fr.gouv.agriculture.o2</groupId>
            <artifactId>o2-kernel</artifactId>
            <version>${kernel.version}</version>
        </dependency>
        <dependency>
            <groupId>fr.gouv.agriculture.o2</groupId>
            <artifactId>o2-kernel-spring</artifactId>
            <version>${kernel.impl.version}</version>
        </dependency>
        <dependency>
            <groupId>fr.gouv.agriculture.o2</groupId>
            <artifactId>o2-kernel-camel</artifactId>
            <version>${kernel.impl.version}</version>
        </dependency>
        <dependency>
            <groupId>fr.gouv.agriculture.o2</groupId>
            <artifactId>o2-transaction-api</artifactId>
            <version>${o2-transaction-api.version}</version>
        </dependency>
        <dependency>
            <groupId>fr.gouv.agriculture.o2</groupId>
            <artifactId>o2-transaction-impl</artifactId>
            <version>${o2-transaction-impl.version}</version>
        </dependency>
        <dependency>
            <groupId>fr.gouv.agriculture.o2</groupId>
            <artifactId>o2-transaction-jdbc</artifactId>
            <version>${o2-transaction-jdbc.version}</version>
        </dependency>
        <dependency>
            <groupId>fr.gouv.agriculture.o2</groupId>
            <artifactId>o2-jdbc-support</artifactId>
            <version>${o2-jdbc-support.version}</version>
        </dependency>
        <dependency>
            <groupId>fr.gouv.agriculture.o2</groupId>
            <artifactId>o2-io</artifactId>
            <version>${o2-io.version}</version>
        </dependency>

        <!-- Apache Commons -->
        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-lang3</artifactId>
            <version>${commons-lang3.version}</version>
        </dependency>
        <dependency>
            <groupId>commons-fileupload</groupId>
            <artifactId>commons-fileupload</artifactId>
            <version>1.1.1</version>
        </dependency>
        <dependency>
            <groupId>commons-validator</groupId>
            <artifactId>commons-validator</artifactId>
            <version>1.3.1</version>
            <exclusions>
                <exclusion>
                    <groupId>commons-logging</groupId>
                    <artifactId>commons-logging</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>commons-collections</groupId>
            <artifactId>commons-collections</artifactId>
            <version>${commons-collections.version}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.camel</groupId>
            <artifactId>camel-quartz</artifactId>
            <version>${camel.version}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.camel</groupId>
            <artifactId>camel-jdbc</artifactId>
            <version>${camel.version}</version>
        </dependency>

        <!-- Database -->
        <dependency>
            <groupId>postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <version>9.0-801.jdbc4</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>p6spy</groupId>
            <artifactId>p6spy</artifactId>
            <version>1.1</version>
            <scope>runtime</scope>
        </dependency>

        <dependency>
            <groupId>c3p0</groupId>
            <artifactId>c3p0</artifactId>
            <version>0.9.1</version>
            <type>jar</type>
        </dependency>

        <!-- Logging -->
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
            <version>${slf4j.version}</version>
        </dependency>
        <!-- Bridge pour intercepter les appels JCL tiré par commons-httpclient -->
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>jcl-over-slf4j</artifactId>
            <version>${slf4j.version}</version>
            <scope>runtime</scope>
        </dependency>
        <!-- Implémentation de SLF4J vers laquelle tous les logs sont redirigés -->
        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
            <version>${logback.version}</version>
            <scope>runtime</scope>
        </dependency>

        <!-- Tests -->
        <dependency>
            <groupId>fr.gouv.agriculture.o2</groupId>
            <artifactId>o2-junit-support</artifactId>
            <version>${o2junit.version}</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <version>${junit.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.easymock</groupId>
            <artifactId>easymock</artifactId>
            <version>${easymock.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.unitils</groupId>
            <artifactId>unitils-core</artifactId>
            <version>${unitils.version}</version>
            <scope>test</scope>
            <exclusions>
                <!-- Utilisation du bridge jcl-over-slf4j -->
                <exclusion>
                    <groupId>commons-logging</groupId>
                    <artifactId>commons-logging</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>org.unitils</groupId>
            <artifactId>unitils-easymock</artifactId>
            <version>${unitils.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.unitils</groupId>
            <artifactId>unitils-inject</artifactId>
            <version>${unitils.version}</version>
            <scope>test</scope>
        </dependency>

    </dependencies>

    <distributionManagement>
        <repository>
            <uniqueVersion>false</uniqueVersion>
            <id>Commons</id>
            <name>Map Internal Repository</name>
            <url>dav:https://repos.forge.agriculture/repository/commons/</url>
            <layout>default</layout>
        </repository>

        <snapshotRepository>
            <uniqueVersion>false</uniqueVersion>
            <id>Work</id>
            <name>O2 Snapshot Repository</name>
            <url>dav:https://repos.forge.agriculture/repository/o2-work</url>
            <layout>default</layout>
        </snapshotRepository>

        <site>
            <id>${reports.service.server}</id>
            <url>dav:https://${reports.service.server}/${departement}${si}/sial-analyse-risque-agent-calculernoterisque/${url-path}/</url>
        </site>
    </distributionManagement>

</project>
